{
	"name": "ftcoa-db",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --remote --env dev",
		"start": "wrangler dev --env dev"
	},
	"devDependencies": {
		"wrangler": "^4.24.0"
	},
	"dependencies": {
		"hono": "^4.9.2",
		"tslib": "^2.8.1"
	}
}
